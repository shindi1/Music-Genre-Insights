# Core data
pandas>=2.0.0,<3.0.0
numpy>=1.24.0,<2.0.0
pyarrow>=14.0.0           # parquet IO

# Text cleaning
ftfy>=6.1.0               # mojibake / encoding fixes
langdetect>=1.0.9         # language detection
unidecode>=1.3.6          # ascii fallback
rapidfuzz>=3.5.0          # fast fuzzy string matching for cross-dataset joins

# ML / preprocessing
scikit-learn>=1.3.0       # train/test split, StandardScaler, etc.
imbalanced-learn>=0.11.0  # optional: SMOTE / RandomUnderSampler

# APIs (supplemental scraping)
lyricsgenius>=3.0.0       # Genius lyrics scraping
spotipy>=2.23.0           # Spotify Web API (search/metadata only post-Nov 2024)
requests>=2.31.0
tenacity>=8.2.0           # retry logic with backoff

# Logging / CLI
tqdm>=4.66.0              # progress bars
python-dotenv>=1.0.0      # load API keys from .env
click>=8.1.0              # cleaner CLI than argparse

# Dev / testing
pytest>=7.4.0
pytest-cov>=4.1.0
